package functioneg

object Parameter {
  def main(args: Array[String]) = {
    sum(22,33)           // Calling function
  }
  def sum(a:Int,b:Int)  ={        // Defining a function
    println(a+b)
  }
}
